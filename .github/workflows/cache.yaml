name: Caching

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    
    container:
      image: maven

    steps:
    - uses: actions/checkout@v3
    
    - name: path check
      run: ls -a ~/.m2/repository

    - name: Cache local Maven repository
      uses: actions/cache@v3
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    - name: maven version
      run: mvn -v

    - name: maven compile
      run: mvn compile

#    - name: maven install
#      run: mvn install
